/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { StudentDto } from '../../models/student-dto';

export interface StudentCrudControllerRemove$Params {
  id: number;
}

export function studentCrudControllerRemove(http: HttpClient, rootUrl: string, params: StudentCrudControllerRemove$Params, context?: HttpContext): Observable<StrictHttpResponse<StudentDto>> {
  const rb = new RequestBuilder(rootUrl, studentCrudControllerRemove.PATH, 'delete');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<StudentDto>;
    })
  );
}

studentCrudControllerRemove.PATH = '/api/v1/crud-student/{id}';

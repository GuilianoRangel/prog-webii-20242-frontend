/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageStudentListDto } from '../../models/page-student-list-dto';
import { SearchFieldValue } from '../../models/search-field-value';

export interface StudentCrudControllerSearchFieldsActionPage$Params {
  page?: number;
  size?: number;
  sort?: Array<string>;
      body: Array<SearchFieldValue>
}

export function studentCrudControllerSearchFieldsActionPage(http: HttpClient, rootUrl: string, params: StudentCrudControllerSearchFieldsActionPage$Params, context?: HttpContext): Observable<StrictHttpResponse<PageStudentListDto>> {
  const rb = new RequestBuilder(rootUrl, studentCrudControllerSearchFieldsActionPage.PATH, 'post');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('sort', params.sort, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageStudentListDto>;
    })
  );
}

studentCrudControllerSearchFieldsActionPage.PATH = '/api/v1/crud-student/search-fields/page';
